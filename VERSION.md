# Version 1.0.0 — Baseline

发布日期：2026-08-23  
状态：基准版本（Baseline）

## 版本定位

`1.0.0` 是 Edge640/Edge680 PVE 风扇控制项目的首个可复现基准版本，用于在其他开发机器上克隆、验证和继续迭代。

## 基准功能

- 自动加载 `lm75`、`tc654` 内核模块。
- 在 `i2c-0` 实例化 LM75 `0x4a` 与 TC654 `0x1b`。
- 按 hwmon 设备名称定位 TC654，不依赖可变的 `hwmonN` 编号。
- 从 `coretemp-isa-0000` 读取 `Package id 0` CPU 温度。
- 通过 TC654 `pwm1` 控制风扇并尝试设置 `pwm1_mode=1`。
- 45°C 以下 PWM 0，45–60°C 线性映射，60°C 以上 PWM 255。
- 升温立即升速，降温延迟 120 秒。
- systemd timer 每约 10 秒运行一次，`AccuracySec=1s`。
- 使用文件锁避免控制任务并发。
- 提供一键安装和卸载脚本。

## 已解决问题

- 修复 awk 对 CPU 温度中 `+` 字符的解析错误。
- 修复降速时间戳每轮刷新、导致永不降速的问题。
- 修复 systemd timer 默认精度造成控制周期接近一分钟的问题。
- 支持 PVE root 环境不依赖 `sudo` 安装。
- 固化 Linux 脚本可执行权限。

## 实机验证摘要

- 平台：Edge680，Proxmox VE 9.2 / Debian 13。
- 压测：`stress-ng --cpu 16 --timeout 120s`，16 worker，失败数 0。
- CPU Package：约 52°C 升至 70°C。
- 高温阶段：PWM 255；风扇约 8,000–9,500 RPM。
- 控制周期：约 10–11 秒。
- 压力解除后：按 120 秒降速延迟策略执行。

## 兼容范围

- 已验证：Edge680。
- 目标兼容：Edge640，以及使用相同 LM75 + TC654 设计的 OEM 设备，例如 Dell VEP1445。
- 未经逐台验证的 OEM 型号不得仅凭型号说明直接投产。

## 已知限制

- 不自动探测并确认 I²C 地址；默认采用基准硬件地址。
- 不提供 Web UI 或 PVE GUI 集成。
- 不提供多温度源取最大值策略。
- 不包含日志轮转配置。
- `sensors` 可能错误显示超过 100% 的 TC654 PWM 百分比。
- 实际 PWM 读回值可能受 TC654/驱动量化影响。

## 后续版本建议

- `1.0.x`：仅修复缺陷、文档和兼容性，不改变默认温控曲线。
- `1.1.0`：增加配置文件、安装前硬件探测、日志轮转和自检命令。
- `2.0.0`：允许改变默认温控模型或引入守护进程/多传感器控制。

## 版本规则

项目采用语义化版本：`主版本.次版本.修订版本`。

- 主版本：存在不兼容的控制行为变化。
- 次版本：向后兼容的新功能。
- 修订版本：向后兼容的错误修复和文档完善。

